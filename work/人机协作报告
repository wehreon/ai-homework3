人机协作报告：NYC 出行数据智能问答系统
一、AI 交互日志
M1：数据处理与特征工程
关键交互：我要求 AI 构建数据质量报告（缺失率、异常值统计）、清洗数据并说明理由、提取时间特征及设计 2 个衍生特征。
AI 输出了完整的 DataProcessor 类代码，包括缺失值统计、异常值检测（车费为负、时间倒流等）。
我采纳了 AI 推荐的"平均行程速度"作为第一个衍生特征，它能直观反映交通拥堵。拒绝了 AI 设计的第二个衍生特征"行程方向类型"，因为该特征需要额外的 taxi_zone_lookup.csv 外部数据做行政区映射，增加了项目复杂度。
我主动提出更换，最终采纳了"人均行程距离"这个完全基于现有字段就能生成的特征。AI倾向于把项目做的尽善尽美，但是人是会懒得动的。

M2:分析与可视化
关键交互：我要求完成 4 项分析（出行需求规律、区域热度、车费因素、自选一项），所有图表保存到 outputs 目录。
AI 生成了完整的分析代码。但在运行后我发现了严重错误：速度范围显示 0.0 到 2160.0 mph，这在城市道路里面根本不可能。
我是查看了特征工程打印的输出日志，注意到速度的最大值异常。AI 分析后指出是计算 trip_distance / trip_duration 时部分记录的时长极短导致速度暴增。
我们共同决定在计算后添加过滤逻辑：速度超过 80 mph 的记录视为异常值，替换为 NaN。修复后速度范围恢复正常（0.0 到 79.8 mph）。自选分析部分我完全采纳了 AI 的建议，增加了出行效率分析，用箱线图和分时速度曲线展示拥堵模式。

M3:预测模型
关键交互：我要求用神经网络预测某区域某时段的需求量，并与随机森林做对比实验。
由于我的电脑使用 Python 3.14，不兼容 TensorFlow，我只能改用 PyTorch。
第一次训练结果很拉跨：神经网络 MAE 高达 150.22，远高于随机森林的 19.30，Loss 曲线在 65000 左右震荡不收敛。
AI 分析后指出这是因为区域 ID 只有 4 个连续特征，神经网络无法理解类别信息。我们共同执行了优化方案：对 PULocationID 进行 One-Hot 编码（特征从 4 维扩增到 256 维），同时降低学习率、增加训练轮数。
优化后神经网络 MAE 降至 30.84，反超了随机森林的 77.48。这次经历让我深刻认识到特征工程对深度学习的决定性作用。

M4:问答接口
关键交互：我要求实现命令行问答循环，通过解析自然语言关键词匹配意图，支持 5 种以上问题类型，并选做接入大模型 API。
AI 设计了基于正则表达式的关键词提取器和意图识别器，支持时段查询、区域排名、拥堵分析、费用查询和需求预测五种类型。
但是AI在星期映射上出现了错误：AI 把"周日"错误映射为 0（应为 6），我问的是周三，它回答的是周2。
我通过测试发现问题后，AI 修正了 day_map 字典，并增加了 day_display 反向映射，使回复显示中文星期名称而非内部数字。
选做部分我接入了 DeepSeek 大模型 API，用于处理规则匹配不到的问题。
System Prompt 设计经历了三次迭代：
V1 版过于简单，回复太泛；
V2 版加入了数据背景但模型会编造具体数字；
V3 版明确要求结合数据背景且回答尽量简短，效果最好。
在调试过程中我不小心将 API Key 提交到了 GitHub，发现后立即在 DeepSeek 平台删除了泄露的 Key 并重新生成，将 Key 改为通过环境变量传入代码。

二、三阶段对比：以"速度异常值过滤"为例
Native 版：如果完全独立编写，我会直接写 speed = distance / time，但是会出现忘记处理除以零或极小时间单位等边界情况，导致程序出现无穷大值或崩溃。需要自己额外花时间做防御性编程。
Prompt 版：如果给 AI 一个笼统的 prompt 如"帮我写速度计算"，它可能输出一段教科书级别的通用代码，但与实际 DataFrame 的列名、数据格式不匹配，需要我自己做大量适配工作。
Vibe 版：实际对话中我只用说"速度怎么到 2160 mph 了？帮我过滤一下异常"，AI 不仅能立刻给出 df.loc 这样的可直接运行的代码，还能在注释里解释为什么选 80 mph 作为阈值。
Vibe 版效率极高，但关键是我必须通过检查 describe 输出来验证修复效果，这个验证环节必须由人来完成。

三、反思：对 AI 工具能力边界的新认识
完成这次作业后，我对 AI 编程工具有了更深刻的认识。
第一个，AI 极其擅长处理"已知的已知"问题。对于 Pandas、PyTorch、matplotlib 这类成熟库的标准用法，它能很快生成正确代码，准确率很高。但当遇到 Python 3.14 和 TensorFlow 这种环境兼容性问题时，它只能给出标准答案而无法感知实际运行环境的限制。这时就需要人来推动技术决策，换用 PyTorch。
然后，AI 缺乏物理常识和业务直觉。它只会机械地执行距离除以时间的计算，不会主动怀疑城市里能不能跑出三倍音速。人必须通过检查中间结果来发现这类隐蔽错误。目前阶段是必须让人和AI分工办事。
但我们应该意识到，真正的核心创造力依然在人身上。当行政区域映射太麻烦时，决定把特征换成人均距离的是人；当神经网络效果被随机森林碾压时，决定用 One-Hot 编码优化而不是无脑加深网络层的也是人。
虽然用自然语言驱动编程极大地提高了编码效率，但提问的精准度、架构的整体把控和最终质量的审判权，始终在开发者自己手中。
总结下来，这次作业让我体会到 AI 真是太好用了（，但是它离不开人的制约。